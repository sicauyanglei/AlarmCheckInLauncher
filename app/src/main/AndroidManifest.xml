<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 拉起其他 App 所需的基础权限 -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <!-- 上传日志到 GitHub -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 开机自启 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- 前台保活服务 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- 请求加入电池优化白名单 -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Android 13+ 前台服务通知需要运行时申请 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 闹钟响铃时唤醒屏幕 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- 强制把目标 App 任务移到前台（覆盖在闹钟全屏界面之上） -->
    <uses-permission android:name="android.permission.REORDER_TASKS" />
    <!-- 系统级窗口覆盖：允许在其他 App 之上显示（覆盖闹钟全屏界面） -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- 管理任务栈（关闭/移动其他 App 任务，需用户授权） -->
    <uses-permission android:name="android.permission.MANAGE_TASKS"
        tools:ignore="ProtectedPermissions" />
    <!-- 悬浮窗（部分厂商 ROM 需要单独授权） -->
    <uses-permission android:name="android.permission.INTERNAL_SYSTEM_WINDOW"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AlarmCheckInLauncher">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".RuleListActivity"
            android:exported="false"
            android:label="@string/title_rule_list" />

        <activity
            android:name=".AppPickerActivity"
            android:exported="false"
            android:label="@string/title_app_picker" />

        <activity
            android:name=".GithubSettingsActivity"
            android:exported="false"
            android:label="@string/title_github_settings" />

        <!-- 透明代理 Activity：闹钟响铃时唤醒屏幕、越过锁屏、拉起目标 App 到前台 -->
        <activity
            android:name=".LaunchProxyActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:theme="@style/Theme.LaunchProxy" />

        <!-- 开机自启接收器 -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- 前台保活服务 -->
        <service
            android:name=".GuardService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keep alarm listener alive to auto-launch check-in app on alarm ring" />
        </service>

        <!--
          闹钟监听服务：通过 NotificationListenerService 监听系统闹钟 App 的通知，
          当检测到「闹钟正在响铃」类通知时，拉起用户配置的打卡 App。
          系统会绑定该服务并保持其常驻，无需额外前台服务。
        -->
        <service
            android:name=".AlarmNotificationListener"
            android:exported="false"
            android:label="@string/listener_service_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>

</manifest>
